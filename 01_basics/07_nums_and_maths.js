// const score = 500
// console.log(score);

// const balance = new Number(1000)
// console.log(balance);

// console.log(balance.toString().length);
// console.log(balance.toFixed(2)); //fixed -> give the 2(n) decimal value

// const otherNumber = 119.915191
// console.log(otherNumber.toPrecision(4)); //use precision after 4(n) numbers

// const hundreds = 1000000
// console.log(hundreds.toLocaleString()); //use comma in big numbers according to US system
// console.log(hundreds.toLocaleString('en-IN')); //Indian system

// ************* Maths ****************************

console.log(Math);
console.log(Math.abs(-4)); //gives absolute value of number
console.log(Math.round(4.6)); //round of the number 
console.log(Math.ceil(4.2)); //round off to upper value always
console.log(Math.floor(4.9)); //round off to lower value always
console.log(Math.min(4, 3, 2, 1, 5)); //find min in an array
console.log(Math.max(4, 3, 2, 5, 2)); //find max in an array

console.log(Math.random()); //random -> always return some value between 0 & 1
console.log((Math.random() * 10) + 1);
 
console.log(Math.floor((Math.random()*10) + 1)); //

const min = 10
const max = 20

console.log(Math.floor(Math.random() * (max - min)) + min); //it gives value always greater than min and less than max





